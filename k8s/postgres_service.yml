apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: postgres-service
spec:
  selector:
    app: postgres-service
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
