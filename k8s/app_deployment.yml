apiVersion: apps/v1
kind: Deployment
metadata:
  name: rss-feeder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rss-feeder
  template:
    metadata:
      labels:
        app: rss-feeder
    spec:
      containers:
        - name: rss-feeder
          image: 'dylanwe/rss-feeder:latest'
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: rss-feeder
          envFrom:
            - configMapRef:
                name: rss-feeder-secret
