apiVersion: v1
kind: PersistentVolume
metadata:
  name: sqlite-volume
  namespace: rss-feeder
  labels:
    type: local
    app: rss-feeder
spec:
  storageClassName: local-path
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/sqlite

