apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqlite-volume-claim
  namespace: rss-feeder
  labels:
    app: sqlite
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi

